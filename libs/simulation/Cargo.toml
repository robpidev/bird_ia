[package]
name = "lib-simulation"
version = "0.1.0"
edition = "2024"

[dependencies]
nalgebra = {version = "0.34.1", features = ["rand-no-std"]}
rand = "0.9.2"
lib-neural-network = {path="../neural-network"}

[dev-dependencies]
test-case = "3.3.1"
